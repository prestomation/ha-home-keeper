import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// Developer Guide sidebar — the equivalent of docs/INTEGRATING.md, expanded as
// the architecture/events content is ported over in a follow-up step.
const sidebars: SidebarsConfig = {
  developerSidebar: ['integrating'],
};

export default sidebars;
