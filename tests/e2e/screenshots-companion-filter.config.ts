/** Config for the companion-filter screenshot capture (see the .capture.ts beside). */
import { captureConfig } from './capture-config';

export default captureConfig('screenshots-companion-filter.capture.ts', {
  timeout: 120_000,
});
