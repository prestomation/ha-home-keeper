/** Config for the one-off i18n screenshot capture (see screenshots-i18n.capture.ts). */
import { captureConfig } from './capture-config';

export default captureConfig('screenshots-i18n.capture.ts');
