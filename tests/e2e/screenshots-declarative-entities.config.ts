/** Config for the one-off declarative companion entity-name screenshot capture (#377). */
import { captureConfig } from './capture-config';

export default captureConfig('screenshots-declarative-entities.capture.ts');
